llvm-array-check-pass
=====================

An LLVM pass that generates array checks on all array access and optimizes the overhead of the checks

:)


some useful commands

	llvm-dis	- llvm diassembler
	lli		- llvm interpreter
	llc		- llvm compiler
